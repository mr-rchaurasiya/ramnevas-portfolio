# Frontend Context Folder

This folder is used for global state management using React Context API:
- **AuthContext.jsx**: Manages JWT token authentication, login status, user role, and session expiration.
